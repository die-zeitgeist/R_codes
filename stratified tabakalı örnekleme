Bunlar daha modern:
library(dplyr)
set.seed(26)
n <- 20

bro <- motiv_factor %>%
  slice_sample(n = n)




table(motiv_factor$bolum)
table(tabakali_veri$bolum)

library(sampling)
library(dplyr)

table(motiv_factor$bolum)

motiv_temiz <- motiv_factor %>%
  filter(!is.na(bolum)) %>%
  arrange(bolum)
toplam_n <- 20
motiv_temiz <- motiv_factor %>%
  filter(!is.na(bolum)) %>%
  arrange(bolum)
boyutlar <- round(toplam_n * table(motiv_temiz$bolum) / nrow(motiv_temiz))
orneklem_indexleri <- strata(motiv_temiz, 
                             stratanames = "bolum", 
                             size = boyutlar, 
                             method = "srswor")
tabakali_veri <- getdata(motiv_temiz, orneklem_indexleri)



küme örnekleme
library(dplyr)

set.seed(42)
motiv_temiz <- motiv_factor %>%
  filter(!is.na(bolum))

secilen_kumeler <- sample(unique(motiv_temiz$bolum), size = 2)

kume_orneklem <- motiv_temiz %>%
  filter(bolum %in% secilen_kumeler)

table(kume_orneklem$bolum)

n_kisi_basina <- 10 

kume_orneklem_2_asama <- motiv_factor %>%
  filter(!is.na(bolum)) %>%
  filter(bolum %in% sample(unique(bolum), size = 2)) %>%
  group_by(bolum) %>%
  slice_sample(n = n_kisi_basina) %>%
  ungroup()

table(kume_orneklem_2_asama$bolum)




















library(dplyr)
library(purrr)

oranlar <- c(
  "Kitap"                             = 0.30,
  "Ünite Özeti"                       = 0.30,
  "Canlı Ders Kayıtları"              = 0.25,
  "Çıkmış Sınav Soruları AÖF Dönem Sonu" = 0.15
)

n_total <- 2000

hedefler <- tibble(
  material = names(oranlar),
  oran     = as.numeric(oranlar)
) %>%
  mutate(n_hedef = round(oran * n_total))
hedefler


a2_filt <- a2 %>%
  filter(material %in% names(oranlar))

# Her kategoriyi ayrı data.frame olarak ayır
a2_split <- split(a2_filt, a2_filt$material)

# Split’teki her kategori için hedef n’leri sıraya diz
n_vec <- hedefler$n_hedef[match(names(a2_split), hedefler$material)]
n_vec


set.seed(123)  # reproducibility için, istersen

sampled <- map2_df(a2_split, n_vec, ~ slice_sample(.x, n = .y))


sampled %>%
  count(material) %>%
  mutate(oransal = n / sum(n))



